[project]
name = "pdflib"
version = "0.1.0"
description = "PDF library management CLI"
requires-python = ">=3.11"
dependencies = [
    "pypdf>=4.0",
    "ebooklib>=0.18",
]

[project.scripts]
pdflib = "pdflib:main"
